'use client'

// Trunk Shows module — top-level nav for sales reps, partners,
// admins. Phase 2 wires the route + sidebar entry; the real CRUD
// + variable hours + special requests + appointments + spiffs
// come in Phases 10–13.

import PlaceholderPage from './PlaceholderPage'

export default function TrunkShows() {
  return (
    <PlaceholderPage
      title="🛍️ Trunk Shows"
      phase="Sales Rollout · Phase 10 of 16"
      blurb="Trunk Shows — store + dates + assigned rep, per-day hours, special requests routed to office staff, customer appointments (Trunk Customer Bookings), and spiff payouts. Lands across Phases 10–13."
    />
  )
}
