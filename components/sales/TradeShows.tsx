'use client'

// Trade Shows module — top-level nav for sales reps, partners,
// admins. Phase 2 wires the route + sidebar entry; the real CRUD
// + booth costs + lead capture + appointments come in Phases 3–9.

import PlaceholderPage from './PlaceholderPage'

export default function TradeShows() {
  return (
    <PlaceholderPage
      title="🎪 Trade Shows"
      phase="Sales Rollout · Phase 3 of 16"
      blurb="Trade Shows module — venue, dates, booth number, staff assignments, booth cost breakdown, lead capture, and booth appointments. Lands in Phase 3."
    />
  )
}
